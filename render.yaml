services:
    - type: web
      name: chatnow
      env: static
      buildCommand: corepack enable pnpm && pnpm install --frozen-lockfile && bash render-build.sh
      staticPublishPath: artifacts/chatnow/dist/public
      routes:
        - type: rewrite
          source: /*
          destination: /index.html
  