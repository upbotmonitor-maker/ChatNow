services:
    - type: web
      name: chatnow
      env: static
      buildCommand: npm install -g pnpm@9 && pnpm install --frozen-lockfile && bash render-build.sh
      staticPublishPath: artifacts/chatnow/dist/public
      routes:
        - type: rewrite
          source: /*
          destination: /index.html
  